/**
 * 1. load total products data 
 * 2. decide on the number of items per page
 * 3. calculate total page (const totalPages = Math.ceil(totalProducts / itemsPerPage);)
 * 4. create page button based on total products
 * 5. 
 * */ 